
# ON HOLD:  

 * do all of the draped models ( idk how im gonna do this, but , . well idk i need to figure this out )  
 * do all of the models with multiple bodies  
 * do the `dead_male_sittingchair.mdl`  
 * do all of the models that are only body parts ( like `dead_male_legs_01.mdl` )

# if a model is marked as a duplicate ( as can be seen via duplicates.txt ), it will not be listed. only the original will be  

# TESTED MODELS ( test everything not on this list )  

 * `ceda/`  
    * `ceda_msgboard_a.mdl`  
    * `ceda_table_a.mdl`  
    * `ceda_table_b.mdl`  
    * `ceda_truck_a.mdl`  
    * `ceda_truck_b.mdl`  

 * `chm_fallensurvivor.mdl`
 * `dead_female_civilian_02.mdl`
 * `dead_military_male01.mdl`
 * `dead_male_suit_01.mdl`
 * `parachutist.mdl`
 * `dead_male_sittingchair.mdl`
 * `dead_female_civilian_draped_01.mdl`
 * `dead_female_civilian_draped_02.mdl`
 * `dead_male_civilian_draped_01.mdl`
 * `common_worker_male01_fence01.mdl`
 * `dead_female_civilian_01.mdl`
 * `dead_female_teen_01.mdl`
 * `dead_male_civilian_01.mdl`
 * `dead_male_civilian_02.mdl`
 * `dead_male_civilian_body.mdl`
 * `dead_male_torso_01.mdl`
 * `dead_male_legs_01.mdl`
 * `dead_police_male01.mdl`

---

# UNUSED MODELS ( these are/will be ported, but cannot be verified to make sure they show up properly ( maybe make a test map? ) )  

 * `riot/`  
    * `riot_msgboard_a.mdl`  
    * `riot_table_a.mdl`  
    * `riot_table_b.mdl`  
    * `riot_truck_a.mdl`  
    * `riot_truck_b.mdl`  

 * `dead_female02_suit.mdl`  
 * `dead_male_tsaagent_01.mdl`  

## TECHNICALITIES WITH UNUSED MODELS LIST  

 * `common_male_fence01.mdl` is technically unused, but it is a duplicate of `cemetary/fence_a.mdl` ( which is used )  
 * `bodies128_fresh_b.mdl` is technically unused, but it is a duplicate of `bodies128_b.mdl` ( which is used )  


